import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke placeholder', (WidgetTester tester) async {
    // Full app requires dotenv + Supabase init in main(); run integration tests on device.
    expect(true, isTrue);
  });
}
